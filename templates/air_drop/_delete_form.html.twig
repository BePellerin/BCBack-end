<form method="post" action="{{ path('app_air_drop_delete', {'id': air_drop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ air_drop.id) }}">
    <button class="btn">Delete</button>
</form>
