<form method="post" action="{{ path('app_drop_delete', {'id': drop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ drop.id) }}">
    <button class="btn">Delete</button>
</form>
