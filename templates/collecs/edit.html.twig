{% extends 'base.html.twig' %}

{% block title %}Edit Collecs{% endblock %}

{% block body %}
<section class="container">

    <h1>Edit Collecs</h1>

    {{ include('collecs/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_collecs_index') }}">back to list</a>

{{ include('collecs/_delete_form.html.twig') }}</section>

{% endblock %}
{% block stylesheets %}
	<style>

		label {
			display: block;
			width: 150px;
			color: #0984e3;
		}
		input {
			width: 300px;
		}
		.btn {
			background-color: #54a0ff;
			color: white;
			margin-top: 5px;
		}
	</style>
{% endblock %}
