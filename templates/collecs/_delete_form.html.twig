<form method="post" action="{{ path('app_collecs_delete', {'id': collec.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collec.id) }}">
    <button class="btn">Delete</button>
</form>
